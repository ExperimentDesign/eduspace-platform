# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Choose one connection string by uncommenting it:
# - Local MySQL (Development): Use for local development with Docker
# - Railway (Production): Use for deployed environment

# --- LOCAL MYSQL (Development) ---
MYSQL_ROOT_PASSWORD=your_root_password
MYSQL_DATABASE=eduspacedb
MYSQL_USER=eduspace
MYSQL_PASSWORD=your_mysql_password
MYSQL_PORT=3308

ConnectionStrings__DefaultConnection=server=localhost;port=3308;user=eduspace;password=your_mysql_password;database=eduspacedb;AllowPublicKeyRetrieval=true;SslMode=none

# --- Railway (Production) ---
# Uncomment the line below to use production database
#ConnectionStrings__DefaultConnection=server=your-railway-host;port=3306;user=root;password=your_production_password;database=railway;AllowPublicKeyRetrieval=true;SslMode=none

# =============================================================================
# EMAIL CONFIGURATION (SMTP)
# =============================================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Allowed origins (comma-separated). Leave empty to allow all origins in development.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://localhost:4200

# SendGrid Configuration
SENDGRID_API_KEY=your_sendgrid_api_key_here
SENDGRID_FROM_EMAIL=noreply@yourdomain.com
SENDGRID_FROM_NAME=EduSpace Platform
